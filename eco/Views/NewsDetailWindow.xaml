<Window x:Class="eco.Views.NewsDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Новость" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <materialDesign:PackIcon Kind="Newspaper" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock Grid.Column="1" Text="Подробности новости" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Grid.Column="2" Style="{DynamicResource MaterialDesignIconButton}" Click="CloseButton_Click">
                    <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                </Button>
            </Grid>
        </materialDesign:ColorZone>

        <!-- Содержимое -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="32">
                <!-- Заголовок новости -->
                <TextBlock x:Name="TitleTextBlock" FontSize="24" FontWeight="Bold" 
                          TextWrapping="Wrap" Margin="0,0,0,16"/>

                <!-- Дата публикации -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                    <materialDesign:PackIcon Kind="Calendar" Width="16" Height="16" 
                                           VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.7"/>
                    <TextBlock x:Name="DateTextBlock" VerticalAlignment="Center" Opacity="0.7"/>
                </StackPanel>

                <!-- Изображение -->
                <Border x:Name="ImageBorder" MaxHeight="300" Margin="0,0,0,24" 
                       CornerRadius="8" Visibility="Collapsed">
                    <Image x:Name="NewsImage" Stretch="Uniform"/>
                </Border>

                <!-- Содержание -->
                <TextBlock x:Name="ContentTextBlock" TextWrapping="Wrap" 
                          LineHeight="24" FontSize="14"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignCardBackground}" Padding="16">
            <Button Content="Закрыть" Style="{DynamicResource MaterialDesignRaisedButton}" 
                   HorizontalAlignment="Right" Click="CloseButton_Click"/>
        </Border>
    </Grid>
</Window>
