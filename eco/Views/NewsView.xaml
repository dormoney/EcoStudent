<UserControl x:Class="eco.Views.NewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="Новости" FontSize="28" FontWeight="Bold" Margin="0,0,0,16"/>
            <TextBlock Text="Последние новости об экологических инициативах" FontSize="16" Opacity="0.7" Margin="0,0,0,32"/>

            <!-- Список новостей -->
            <ItemsControl x:Name="NewsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,0,0,16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="200"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Изображение -->
                                <Border Grid.Row="0" Background="{DynamicResource PrimaryHueLightBrush}" CornerRadius="4,4,0,0">
                                    <Grid>
                                        <Image Source="{Binding ImagePath}" Stretch="UniformToFill" 
                                              Visibility="{Binding ImagePath, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        <materialDesign:PackIcon Kind="Newspaper" Width="64" Height="64" 
                                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                                               Foreground="White" Opacity="0.7"
                                                               Visibility="{Binding ImagePath, Converter={StaticResource InverseNullToVisibilityConverter}}"/>
                                    </Grid>
                                </Border>

                                <!-- Содержимое -->
                                <StackPanel Grid.Row="1" Margin="24">
                                    <!-- Заголовок и дата -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" 
                                                  TextWrapping="Wrap" VerticalAlignment="Top"/>
                                        
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                                            <materialDesign:PackIcon Kind="Calendar" Width="16" Height="16" 
                                                                   VerticalAlignment="Center" Margin="0,0,8,0" Opacity="0.7"/>
                                            <TextBlock Text="{Binding PublishDate, StringFormat=dd.MM.yyyy}" 
                                                      VerticalAlignment="Center" Opacity="0.7"/>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Содержание -->
                                    <TextBlock Text="{Binding Content}" TextWrapping="Wrap" 
                                              Opacity="0.8" LineHeight="20" MaxHeight="120"
                                              TextTrimming="CharacterEllipsis"/>

                                    <!-- Кнопка "Читать далее" -->
                                    <Button Content="Читать далее" 
                                           Style="{DynamicResource MaterialDesignFlatButton}"
                                           HorizontalAlignment="Left"
                                           Click="ReadMoreButton_Click"
                                           Tag="{Binding}"
                                           Margin="0,16,0,0"/>
                                </StackPanel>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Сообщение, если нет новостей -->
            <materialDesign:Card x:Name="NoNewsCard" Padding="32" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="NewspaperVariantOutline" Width="64" Height="64" 
                                           HorizontalAlignment="Center" Opacity="0.5" Margin="0,0,0,16"/>
                    <TextBlock Text="Новостей пока нет" FontSize="18" FontWeight="Bold" 
                              HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Следите за обновлениями!" 
                              HorizontalAlignment="Center" Opacity="0.7"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
