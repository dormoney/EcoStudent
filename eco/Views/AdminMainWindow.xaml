<Window x:Class="eco.Views.AdminMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:eco"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="19*"/>
            <ColumnDefinition Width="131*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" Grid.ColumnSpan="2" VerticalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ShieldAccount" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="EcoStudent Admin" FontSize="24" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="AdminNameText" Text="Администратор" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button x:Name="LogoutButton" 
                            Content="Выйти" 
                            Style="{DynamicResource MaterialDesignOutlinedButton}"
                            Click="LogoutButton_Click"/>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>

        <!-- Main Content -->
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,46" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Menu -->
            <materialDesign:Card Grid.Column="0" Margin="8">
                <StackPanel>
                    <Separator/>

                    <ListBox x:Name="NavigationListBox" SelectionChanged="NavigationListBox_SelectionChanged" 
                             SelectedIndex="0" BorderThickness="0" Background="Transparent">
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <materialDesign:PackIcon Kind="ViewDashboard" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Панель управления" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <materialDesign:PackIcon Kind="AccountPlus" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Регистрация пользователей" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <materialDesign:PackIcon Kind="MapMarker" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Управление пунктами" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <materialDesign:PackIcon Kind="CheckCircle" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Подтверждение сдач" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <materialDesign:PackIcon Kind="AccountGroup" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Управление пользователями" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <materialDesign:PackIcon Kind="Gift" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Управление акциями" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <materialDesign:PackIcon Kind="ChartLine" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Статистика" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </StackPanel>
            </materialDesign:Card>

            <!-- Content Area -->
            <materialDesign:Card Grid.Column="1" Margin="8">
                <ContentControl x:Name="ContentArea">
                    <!-- Dashboard Content -->
                    <Grid x:Name="DashboardGrid">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="16">
                                <TextBlock Text="Панель управления администратора" FontSize="18" Opacity="0.7" Margin="0,0,0,32"/>

                                <!-- Statistics Cards -->
                                <Grid Margin="0,0,0,32">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="16">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="TotalUsersText" Text="0" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                            <TextBlock Text="Всего пользователей" Opacity="0.7" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </materialDesign:Card>

                                    <materialDesign:Card Grid.Column="1" Margin="8,0,8,0" Padding="16">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="TotalSubmissionsText" Text="0" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                            <TextBlock Text="Всего сдач" Opacity="0.7" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </materialDesign:Card>

                                    <materialDesign:Card Grid.Column="2" Margin="8,0,8,0" Padding="16">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="PendingVerificationsText" Text="0" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                            <TextBlock Text="Ожидают подтверждения" Opacity="0.7" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </materialDesign:Card>

                                    <materialDesign:Card Grid.Column="3" Margin="8,0,0,0" Padding="16">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="TotalPointsText" Text="0" FontSize="24" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                            <TextBlock Text="Всего баллов выдано" Opacity="0.7" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </materialDesign:Card>
                                </Grid>

                                <!-- Recent Activity -->
                                <TextBlock Text="Последние сдачи" FontSize="20" Margin="0,0,0,16"/>
                                <materialDesign:Card Padding="16">
                                    <StackPanel x:Name="RecentSubmissionsPanel">
                                        <TextBlock Text="Загрузка..." Opacity="0.7" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </materialDesign:Card>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </ContentControl>
            </materialDesign:Card>
        </Grid>

        <!-- Footer -->
    </Grid>
</Window>
