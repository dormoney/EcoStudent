<Window x:Class="eco.Views.SubmissionDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Детали сдачи вторсырья" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Детали сдачи вторсырья" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- User Information -->
                <materialDesign:Card Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Информация о пользователе" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,12"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:" FontWeight="Bold"/>
                            <TextBlock x:Name="UserNameText" Grid.Row="0" Grid.Column="1" Margin="8,0,0,0"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" FontWeight="Bold"/>
                            <TextBlock x:Name="UserEmailText" Grid.Row="1" Grid.Column="1" Margin="8,0,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Группа:" FontWeight="Bold"/>
                            <TextBlock x:Name="UserGroupText" Grid.Row="2" Grid.Column="1" Margin="8,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Submission Information -->
                <materialDesign:Card Padding="16" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Информация о сдаче" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,12"/>
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="ID сдачи:" FontWeight="Bold"/>
                            <TextBlock x:Name="SubmissionIdText" Grid.Row="0" Grid.Column="1" Margin="8,0,0,0"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Тип материала:" FontWeight="Bold"/>
                            <TextBlock x:Name="MaterialTypeText" Grid.Row="1" Grid.Column="1" Margin="8,0,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Вес:" FontWeight="Bold"/>
                            <TextBlock x:Name="WeightText" Grid.Row="2" Grid.Column="1" Margin="8,0,0,0"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Пункт сдачи:" FontWeight="Bold"/>
                            <TextBlock x:Name="RecyclingPointText" Grid.Row="3" Grid.Column="1" Margin="8,0,0,0"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Дата сдачи:" FontWeight="Bold"/>
                            <TextBlock x:Name="SubmissionDateText" Grid.Row="4" Grid.Column="1" Margin="8,0,0,0"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Баллы:" FontWeight="Bold"/>
                            <TextBlock x:Name="PointsText" Grid.Row="5" Grid.Column="1" Margin="8,0,0,0" FontWeight="Bold" Foreground="Green"/>
                        </Grid>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Status Information -->
                <materialDesign:Card Padding="16">
                    <StackPanel>
                        <TextBlock Text="Статус" 
                                   FontSize="16" 
                                   FontWeight="Bold" 
                                   Margin="0,0,0,12"/>
                        
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon x:Name="StatusIcon" 
                                                     Kind="Clock" 
                                                     Width="24" 
                                                     Height="24" 
                                                     VerticalAlignment="Center" 
                                                     Margin="0,0,8,0"/>
                            <TextBlock x:Name="StatusText" 
                                       Text="Ожидает подтверждения" 
                                       FontSize="14" 
                                       FontWeight="Bold" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Закрыть" 
                    Style="{DynamicResource MaterialDesignOutlinedButton}"
                    Click="CloseButton_Click"
                    Margin="0,0,8,0"/>
        </StackPanel>
    </Grid>
</Window>
