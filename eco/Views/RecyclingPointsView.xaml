<UserControl x:Class="eco.Views.RecyclingPointsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="Пункты сдачи вторсырья" FontSize="28" Margin="0,0,0,16"/>
           


            <!-- Список пунктов сдачи -->
            <ItemsControl x:Name="RecyclingPointsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,0,0,16" Padding="20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
                                    
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                      
                                        <TextBlock Text="{Binding Address}" VerticalAlignment="Center" Opacity="0.8"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                       
                                        <TextBlock Text="{Binding WorkHours}" VerticalAlignment="Center" Opacity="0.8"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        
                                        <TextBlock Text="{Binding ContactPhone}" VerticalAlignment="Center" Opacity="0.8"/>
                                    </StackPanel>

                                </StackPanel>
                            </Grid>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Сообщение, если нет результатов -->
            <materialDesign:Card x:Name="NoResultsCard" Padding="32" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center">
                    <materialDesign:PackIcon Kind="MapMarkerOff" Width="64" Height="64" 
                                           HorizontalAlignment="Center" Opacity="0.5" Margin="0,0,0,16"/>
                    <TextBlock Text="Пункты сдачи не найдены" FontSize="18" FontWeight="Bold" 
                              HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock Text="Попробуйте изменить параметры поиска" 
                              HorizontalAlignment="Center" Opacity="0.7"/>
                </StackPanel>
            </materialDesign:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>
