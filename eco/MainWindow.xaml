<Window x:Class="eco.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:eco"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="19*"/>
            <ColumnDefinition Width="131*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" Grid.ColumnSpan="2" VerticalAlignment="Center">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="EcoStudent" FontSize="24" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="UserNameText" Text="Пользователь" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button x:Name="LogoutButton" 
                            Content="Выйти" 
                            Style="{DynamicResource MaterialDesignOutlinedButton}"
                            Click="LogoutButton_Click"/>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>

        <!-- Main Content -->
        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="0,0,0,46" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Menu -->
            <materialDesign:Card Grid.Column="0" Margin="8">
                <StackPanel>
                    <Separator/>

                    <ListBox x:Name="NavigationListBox" SelectionChanged="NavigationListBox_SelectionChanged" 
                             SelectedIndex="0" BorderThickness="0" Background="Transparent">
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <TextBlock Text="Главная" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <TextBlock Text="Сдать вторсырье" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <TextBlock Text="Пункты сдачи" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <TextBlock Text="Акции" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem>
                            <StackPanel Orientation="Horizontal" Margin="8">
                                <TextBlock Text="Мой профиль" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </StackPanel>
            </materialDesign:Card>

            <!-- Content Area -->
            <materialDesign:Card Grid.Column="1" Margin="8">
                <ContentControl x:Name="ContentArea">
                    <!-- Home Page Content -->
                    <Grid x:Name="HomeGrid">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="16">
                                <TextBlock Text="Система для сдачи вторсырья студентами" FontSize="18" Opacity="0.7" Margin="0,0,0,32"/>

                                <!-- Statistics Cards -->
                                <Grid Margin="0,0,0,32">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <materialDesign:Card Grid.Column="0" Margin="0,0,8,0" Padding="16">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="UserPointsText" Text="0 баллов" FontSize="16" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                            <TextBlock Text="Мои баллы" Opacity="0.7" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </materialDesign:Card>

                                    <materialDesign:Card Grid.Column="1" Margin="8,0,8,0" Padding="16">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="SubmissionsCountText" Text="0 сдач" FontSize="16" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                            <TextBlock Text="Всего сдач" Opacity="0.7" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </materialDesign:Card>

                                    <materialDesign:Card Grid.Column="2" Margin="8,0,8,0" Padding="16">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock x:Name="TotalWeightText" Text="0 кг" FontSize="16" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                            <TextBlock Text="Общий вес" Opacity="0.7" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </materialDesign:Card>


                                </Grid>

                                <!-- Recent Activity -->
                                <TextBlock Text="Последняя активность" FontSize="20" Margin="0,0,0,16"/>
                                <materialDesign:Card Padding="16">
                                    <StackPanel x:Name="RecentActivityPanel">
                                        <TextBlock Text="" Opacity="0.7" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </materialDesign:Card>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </ContentControl>
            </materialDesign:Card>
        </Grid>

        <!-- Footer -->
    </Grid>
</Window>
