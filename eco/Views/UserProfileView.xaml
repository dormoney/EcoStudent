<UserControl x:Class="eco.Views.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="Мой профиль" FontSize="28" Margin="0,0,0,32"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Профиль пользователя -->
                <materialDesign:Card Grid.Column="0" Padding="24">
                    <StackPanel Height="330">
                        <!-- Аватар -->
                        <Border Width="120" Height="120" CornerRadius="60" 
                               Background="{DynamicResource PrimaryHueLightBrush}"
                               HorizontalAlignment="Center" Margin="0,0,0,16">
                            <materialDesign:PackIcon Kind="Account" Width="80" Height="80" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   Foreground="White"/>
                        </Border>

                        <!-- Информация о пользователе -->
                        <TextBlock x:Name="UserNameTextBlock" Text="" FontSize="20" FontWeight="Bold" 
                                  HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        
                        <TextBlock x:Name="UserEmailTextBlock" Text="" 
                                  HorizontalAlignment="Center" Opacity="0.7" Margin="0,0,0,8"/>
                        
                        <TextBlock x:Name="UserGroupTextBlock" Text="" 
                                  HorizontalAlignment="Center" Opacity="0.7" Margin="0,0,0,16"/>

                        <!-- Статистика -->
                        <StackPanel>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="1" Text="Баллы" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                <TextBlock x:Name="TotalPointsTextBlock" Grid.Column="2" Text="0" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="1" Text="Сдач" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                <TextBlock x:Name="SubmissionsCountTextBlock" Grid.Column="2" Text="0" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="1" Text="Общий вес" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                <TextBlock x:Name="TotalWeightTextBlock" Grid.Column="2" Text="0 кг" FontWeight="Bold" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>

                        <Separator Margin="0,16,0,16"/>

                        <!-- Дата регистрации -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock x:Name="RegistrationDateTextBlock" Text="" VerticalAlignment="Center" Opacity="0.7"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Детальная информация -->
                <StackPanel Grid.Column="2">
                
                </StackPanel>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
